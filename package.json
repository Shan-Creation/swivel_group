{
  "name": "swivel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run-back": "cd swivel_backend && npm run dev",
    "run-front": "cd swivel_client && npm start",
    "install-all": "npm install && cd swivel_client && npm install && cd .. && cd swivel_backend && npm install",
    "start": "concurrently --kill-others \"npm run run-back\" \"npm run run-front\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "concurrently": "^5.2.0"
  }
}
